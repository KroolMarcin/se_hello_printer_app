lanquage: python

install:
  - make deps
script:
  - make lint
  - make test
  - make docker_build
  - make docker_push
services:
  - docker
deploy:
  provider: heroku
  app: peaceful-escarpment-073634
  api_key: ${HEROKU_API_KEY}